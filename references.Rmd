`r if (knitr:::is_html_output()) '# References {-}'`

```{r include=FALSE}
# generate a BibTeX database automatically for some R packages
knitr::write_bib(c(
  
  # boilerplate ----
  .packages(), 
  'bookdown', 
  'knitr', 
  'rmarkdown',
  
  # mentioned in text ----
  'naniar',
  'datasauRus',
  'nullabor',
  'testthat',
  'pointblank',
  'assertr',
  'renv'
  
  
), 'packages.bib')
```

